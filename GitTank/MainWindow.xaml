<Window x:Class="GitTank.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="GitTank" WindowStartupLocation="CenterScreen"
        Height="450" Width="800"
        MinHeight="450" MinWidth="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>
        <ComboBox Grid.Column="0" Grid.Row="0" x:Name="ComboBoxBranches" Margin="10,10,10,10" IsTextSearchEnabled="True" IsEditable="false"/>
        <TextBox Grid.Column="0" Grid.Row="1" x:Name="TextBoxOutput" IsReadOnly="True" Margin="10,0,10,10" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
        <StackPanel Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" Margin="0, 10, 10, 10">
            <Button Content="Branch" Margin="0, 0, 0, 5" Click="OnButtonBranchClick"/>
            <Button Content="Update" Margin="0,5" Click="OnButtonUpdateClick"/>
            <Button Content="Branches" Margin="0,5" Click="OnButtonBranchesClick"/>
            <Button Content="Checkout" Margin="0,5" Click="OnButtonCheckoutClick"/>
            <Button Content="Sync" Margin="0,5" Click="OnButtonSyncClick"/>
        </StackPanel>
    </Grid>
</Window>
